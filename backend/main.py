from config.settings import APP_NAME, ENVIRONMENT
from config.logger import console

console.print(f"Starting {APP_NAME}...")
console.print(f"Environment: {ENVIRONMENT}")

print("Orbit.Ai backend initialized successfully.")
